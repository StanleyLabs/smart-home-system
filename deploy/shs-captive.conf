# Captive portal DNS: resolve all domains to the hub's hotspot IP.
# Active only when NetworkManager runs a "shared" connection (hotspot mode).
# When the hotspot is down, dnsmasq isn't running so this has no effect.
address=/#/10.42.0.1
